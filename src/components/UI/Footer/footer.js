import React from 'react';
import footerStyles from './footer.module.scss';

const footer = () => {
    return (
        <footer>
            <p>Footer</p>
        </footer>
    );
};

export default footer;
